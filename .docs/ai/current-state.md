# harness-deck — current state

_Loop state only. Shipped-work history → roadmap.md; rationale → decisions.md._

- Branch: main, in sync with origin. **v0.2.15 RELEASED + installed + verified
  2026-07-13** — macOS onboarding batch complete: hdeck cert + doctor, brew
  services persistence, Developer ID signing/notarization, AGENTS.md +
  rewritten install docs. Spec/report:
  `phases/macos-onboarding-{spec,report}.md`.
- Proven on the *shipped* binary: signed+notarized, reached the phone URL with
  **zero manual firewall commands**, `hdeck doctor` 12/12 ok (exit 0) — and
  exit 1 on a never-started install, which v0.2.14 wrongly passed.

## Plan

- (empty — v0.2.15 released + installed + verified; pull the next item from
  roadmap Next/Backlog/Later when starting fresh)

## Blockers

- None.

## Open questions

- None.
